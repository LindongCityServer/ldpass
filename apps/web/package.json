{
  "name": "@ldpass/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm build:api && next build",
    "build:api": "pnpm --filter @ldpass/api build",
    "check": "pnpm typecheck",
    "dev": "pnpm build:api && next dev -p 3200",
    "lint": "pnpm typecheck",
    "start": "next start -p 3200",
    "typecheck": "pnpm build:api && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ldpass/api": "workspace:*",
    "@ldpass/contracts": "workspace:*",
    "@ldpass/ui": "workspace:*",
    "next": "^15.3.4",
    "qrcode": "^1.5.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6"
  }
}
